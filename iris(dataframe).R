length<-c(2.1,3.4,2,5,2,7,2.9)
diameter<-c(0.3,0.5,0.6,0.9,1.1)
V<-length*pi*(diameter/2)2
V

a<-iris
tapply(iris$Sepal.Length, iris$Species, mean)

tapply(iris$Petal.Length, iris$Species, mean)
